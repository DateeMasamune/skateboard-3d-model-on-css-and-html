document.addEventListener("DOMContentLoaded", function (event) {

  const trick = 'impossible'

  const skateboard = document.querySelector('.skateboard')

  skateboard.style.animation = `${trick} 4s linear infinite`

});

/**
 * TRICK LIST
 * 
 * flip
 * impossible
 */